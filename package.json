{
  "name": "mysite",
  "private": true,
  "scripts": {
    "build": "export NODE_OPTIONS=--openssl-legacy-provider && gridsome build",
    "develop": "gridsome develop",
    "explore": "gridsome explore",
    "predeploy": "npm run build",
    "deploy": "export NODE_OPTIONS=--openssl-legacy-provider && gh-pages -d dist"
  },
  "dependencies": {
    "gh-pages": "^6.0.0",
    "gridsome": "^0.7.0"
  }
}
